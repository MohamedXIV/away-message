name: Issue 8 TDD gate

on:
  push:
    branches:
      - sdd/issue-6-verifier

permissions:
  contents: read

jobs:
  tdd:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout exact Issue 8 repair source
        uses: actions/checkout@v4
        with:
          ref: 5c3d58cd23489a096d931b60785af1cea68cef2a
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm ci --legacy-peer-deps
      - name: Home setup inventory GREEN
        run: npx vitest run tests/unit/HomeComputerSetup.test.ts tests/unit/StorePurchase.test.ts tests/unit/SaveRoundTrip.test.ts
      - name: Typecheck
        run: npx tsc --noEmit
