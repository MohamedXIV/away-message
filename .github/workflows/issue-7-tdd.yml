name: Issue 8 TDD gate

on:
  push:
    branches:
      - sdd/issue-6-verifier

permissions:
  contents: read

jobs:
  tdd:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout exact Issue 8 repair source
        uses: actions/checkout@v4
        with:
          ref: 1ae5385816c379d58428dd13beaf794aeaad37c3
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm ci --legacy-peer-deps
      - name: Home assembly GREEN
        run: npx vitest run tests/unit/HomeComputerSetup.test.ts tests/unit/StorePurchase.test.ts
      - name: Typecheck
        run: npx tsc --noEmit
